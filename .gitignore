# Dependências e saídas de build
/node_modules
/.next
/out
/build
*.tsbuildinfo

# Ambiente (o .env.example é documentação: vai versionado)
.env*
!.env.example
.DS_Store
*.stackdump

# Ferramentas locais (config de máquina, não do projeto)
.claude/
.omc/
.ds-sync/
.design-sync/
ds-bundle/
